#include<stdio.h>
int main()
{
    double a,b,c,x,y,z;
    scanf("%lf %lf %lf",&a,&b,&c);

    if(a<b+c && b<a+c && c<a+b)
    {
        x=a+b+c;
        printf("Perimetro = %.1lf\n",x);
    }
    else{
        y=a+b;
        z=(y/2)*c;
        printf("Area = %.1lf\n",z);
    }
    return 0;
}
